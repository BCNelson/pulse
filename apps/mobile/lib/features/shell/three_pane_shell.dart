/// The three top-level tabs in the shell route. Used by
/// `currentTabProvider` in `lib/core/router.dart` to map the current
/// route's path prefix back to a tab enum value.
enum ShellTab { inbox, feed, chat }
