// Package model holds GraphQL models — both gqlgen-generated (models_gen.go)
// and any hand-written types referenced via gqlgen.yml's models map. Hand-
// written types let us bind a GraphQL type to a richer Go struct than the
// generator would produce on its own.
package model
